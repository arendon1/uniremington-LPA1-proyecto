package one.austral.lpa1.model;

/**
 * Categorias del catalogo (RF4: lista de productos organizados por categorias).
 * El nombre viaja como string en la API; la SPA traduce la etiqueta (ES/EN/PT).
 */
public enum Categoria {
	HAMBURGUESAS,
	COMIDAS,
	BEBIDAS,
	POSTRES
}
