# =========================================
# Proyecto Spring - LPA 1
# Stack: Java 21 / Spring Boot 4 / Maven / libsql
# Frontend: Svelte 5 + Vite + TypeScript + UnoCSS
# =========================================

# --- Java / Maven / Spring Boot ---
HELP.md
target/
build/
out/
.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
*.war
*.ear
*.log
hs_err_pid*
replay_pid*

# Spring Boot
spring-boot-devtools.properties

# --- IDEs ---
.idea/
*.iml
*.iws
*.ipr
.vscode/
.project
.classpath
.settings/
nbproject/
nb-configuration.xml

# --- OS / Misc ---
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# --- Frontend (Svelte 5 + Vite) ---
node_modules/
dist/
build/
.svelte-kit/
.vite/
.cache/
coverage/
*.local
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- DB local libsql / secrets ---
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.db
application-local.properties
application-local.yml
secrets/

# --- Password reset logs (offline) ---
reset-links.log

# --- Videos y binarios grandes del material de entregables ---
# Los videos subidos a YouTube no van al repo (binario grande, ~500MB).
# Los archivos de Affinity Designer (.af) y los backups (.bak-prefill)
# son metadata de editor que tampoco se versiona.
*.mp4
*.af
*.bak-prefill

# --- Node / npm (tooling local, no se publica) ---
package.json
package-lock.json

# --- Material local del agente (NO se commitea) ---
# Razón: estos directorios son artefactos del agente. El repo público se
# manufactura con un shadow repo (ver docs/entregables/PUBLICACION-REPO.md).
# `docs/entregables/` ya no se ignora: el shadow repo filtra el contenido
# antes de publicar (regla "shadow repo").
.planes/
.agents/
.pi/
skills-lock.json
AGENTS.md
